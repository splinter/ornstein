/*global describe, it */
'use strict';
var assert = require('assert');
var ornstein = require('../');

describe('ornstein node module', function () {
  it('must have at least one test', function () {
    ornstein();
    assert(false, 'I was too lazy to write any tests. Shame on me.');
  });
});
